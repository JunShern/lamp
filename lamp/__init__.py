"""Robolamp — the Python side of the lamp.

This package is the code that will run on the Raspberry Pi. It consumes the
same ``assets/*.json`` files as the browser simulator (``sim/``); the two
implementations are cross-validated against each other, so behaviour tuned
in the simulator is behaviour the hardware will perform.
"""
